<div class = "container">
    <h1>Posts Index</h1>

  <% if user_signed_in? %>
  <p class="text-right">
    <%= link_to("New", new_post_path, class:"btn btn-primary") %></p>
  <% end %>

  <div class="row">
    <% @posts.each do |post| %>

    <div class="col-4">
      <%= link_to(post) do %>
        <%= image_tag(post.image.url, class:"img-fluid") %>
      </a>
      <% end %>
    </div>
  
  <% end %>
</div>
</div>